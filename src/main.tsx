import React from 'react';
import ReactDOM from 'react-dom/client';
import './styles.css'; // Make sure this matches your CSS filename inside src/

// If you have a primary App component, import it here:
// import App from './App'; 

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    {/* Replace with your main component wrapper if necessary */}
    <div>Portfolio Loaded Successfully</div> 
  </React.StrictMode>,
);